name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: dotnet restore src/Common.Messaging/Common.Messaging.csproj
      - run: dotnet build src/Common.Messaging/Common.Messaging.csproj --configuration Release --no-restore
